export default function page() {
  return (
    <div className="container p-6">
      <h1 className="text-lg font-semibold">Test page (temporary minimal)</h1>
      <p>Used to isolate build issues. Content removed.</p>
    </div>
  );
}
